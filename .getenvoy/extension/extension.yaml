#
# Envoy Wasm extension created with getenvoy toolkit.
#
kind: Extension

name: tetratelabs.filters.network.smtp

category: envoy.filters.network
language: rust

# Runtime the extension is being developed against.
runtime:
  envoy:
    version: wasm:1.15
